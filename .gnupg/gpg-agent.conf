# enables SSH support (ssh-agent)
enable-ssh-support

# writes environment information to ~/.gpg-agent-info
write-env-file
use-standard-socket

# Cache settings
default-cache-ttl 600
max-cache-ttl 7200
